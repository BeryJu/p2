---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "p2.fullname" . }}-pvc-app-storage
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
